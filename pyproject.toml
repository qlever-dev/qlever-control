[build-system]
requires = ["setuptools>=64.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qlever"
description = "Command-line tool for using the QLever graph database"
version = "0.5.47"
authors = [
    { name = "Hannah Bast", email = "bast@cs.uni-freiburg.de" }
]
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.10"

keywords = ["Graph database", "Triplestore", "Knowledge graphs", "SPARQL", "RDF"]

classifiers = [
 "Topic :: Database :: Database Engines/Servers",
 "Topic :: Database :: Front-Ends"
]

# sparql-conformance provides the `sparql_conformance` commands package
# (until it is on PyPI, install it from its repository, e.g.
# `pip install git+https://github.com/ad-freiburg/sparql-conformance`).
dependencies = [ "psutil", "termcolor", "argcomplete", "pyyaml", "rdflib", "requests-sse", "tqdm>=4.60.0", "sparql-conformance>=0.1.0" ]

[project.optional-dependencies]
dev = [ "ruff", "pre-commit" ]

[project.urls]
homepage = "https://github.com/ad-freiburg/qlever"
documentation = "https://docs.qlever.dev"
repository = "https://github.com/ad-freiburg/qlever.git"
bugtracker = "https://github.com/ad-freiburg/qlever/issues"

[project.scripts]
"qblazegraph" = "qlever.qlever_main:main"
"qgraphdb" = "qlever.qlever_main:main"
"qjena" = "qlever.qlever_main:main"
"qlever" = "qlever.qlever_main:main"
"qmdb" = "qlever.qlever_main:main"
"qoxigraph" = "qlever.qlever_main:main"
"qvirtuoso" = "qlever.qlever_main:main"
"sparql_conformance" = "qlever.qlever_main:main"

[tool.setuptools]
package-data = { "qlever" = ["Qleverfiles/*", "evaluation/www/*"] }

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.ruff]
line-length = 79 
[tool.ruff.lint]
extend-select = ["I"]
